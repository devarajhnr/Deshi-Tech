name: Enter_your_address
run-name: Your address is | subject area [${{ inputs.subjectarea }}]
on: 
  workflow_dispatch:
   inputs:
      subjectarea:
        description: Enter your sweet name
        default: ''
        required: true
jobs:
  display_files:
    name: Your address is 
    runs-on: ubuntu-latest
    steps:        
    - name: display output
      run: |
          echo ${{ inputs.subjectarea }}
      shell: bash
