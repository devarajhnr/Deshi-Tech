name: Print my address
on:
  workflow_dispatch:
    inputs:
      your_name:
        type: string
        description: your_name
        default: ''
        required: true
jobs:
  printing_myaddress:
    run-on: ubantu-latest
    steps:
      echo 'Yourname is:'
