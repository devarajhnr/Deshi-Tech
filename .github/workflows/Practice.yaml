name: My first job

on: workflow_dispatch

jobs:
  display_files:
    name: Directeries are 
    runs-on: ubuntu-latest
    steps:        
    - name: display output
      run: |
          dir
      shell: bash
